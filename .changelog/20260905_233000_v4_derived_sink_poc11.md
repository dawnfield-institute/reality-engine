# v4: the derived sink round — energy ledger, ledger severance, Landauer erasure; POC-11 (0/4: the trigger selects the cores)

**Date**: 2026-09-05 · **Branch**: `feat/v4-derived-sink` · **Spec**: `.spec/v4-derived-sink.spec.md`
· **Registered in**: dawn-field-theory Milestone R exp_28 (sealed `bf833113`)

## Instruments (stay)

- **Third-law pressure** (`SECPressure`): mean-entropy repulsion `sec·(S_i+S_j)/2·e^{−r/r0}`; the
  inherited rule had zero antisymmetric part (self-propulsion, C4.4). Pressure impulse ≡ 0 to 7e-6.
- **Energy ledger**: pair potential tabulated from the force's own kernel (65536 points; E1 closed
  form to 4e-7), exact per-force work partition, `loss_drag`, `loss_guard`, `closure_residual`
  every tick (≤ 6e-7 across the whole grid). Interacting / severed / global splits in `PACLedger`.
- **`LedgerSeverance`**: whole-particle decoupling on `min over neighbours within the lattice
  spacing of |S_i − S_j| > τ` (Milestone R exp_15 form); severed particles drift, interact with
  nothing, keep their velocity bit-for-bit; `random` mode replays an event log (the selection
  control). Inert unless `sev_tau`; `CANONICAL_SINK` with sinks off is bit-identical to `CANONICAL`.
- **`LandauerErasure`**: released entropy costs `LN2·|ΔS|` of kinetic energy (fracton's constant,
  named). Inert unless `landauer`. Exploratory arm; ~1% of the pressure work.
- `tests/v4`: 9 → 27 (fail-first, `xfail(strict=True)` until each operator landed); suite 169.
- Named constants only: `LN2`, `PHI`, `XI_ANALYTIC` imported from fracton; runs record `xi_variant`.

## POC-11 (`proof_of_concepts/v4/poc_11_derived_sink/`)

Gates 13/13 (`exp_01`), trigger calibration with removal (`exp_02`, no structure metric by
construction), one-arm runner (`exp_03`: B0/S/D/R/L/SL), aggregator applying the registered frame
(`exp_04`), grid driver (`run_grid.sh`). Proxy grid 36 runs (6 s each), n = 4000 grid 12 runs.

**Result 0/4** against the sealed thresholds: the derived trigger fires only at extrema of the
entropy field, which on this substrate are the collapse cores; severance removes the bound,
connected part and leaves the rest hotter and *less* connected than a random subset at the same
count (3/3 seeds, −2.6 pooled σ at the proxy's τ = 20; a null at n = 4000, τ\* = 10, where half
leaves and S = R); random removal at the onset τ holds more; the matched drag holds as much or more.
On the corrected force the baseline's detonation is ~100× weaker than measured on the self-propelling
rule (KE/|U| 11–18, not 960) and it still cannot hold. **C4.1 option 3 closed; option 2 (derive the
pressure term from the SEC functional) remains.** Journal
`journals/2026-09-05_the-trigger-selects-the-cores.md`.

## Caught on the way

- The drag control was defined on a ratio of *signed* energies that crosses zero as the set unbinds
  (ρ = −5.4 on the smoke test): replaced by the retained kinetic energy per particle, positive-definite,
  one pre-declared refinement.
- The aggregator globbed its own grid file and crashed the second aggregation: excluded by name.
- KA-i's convergence check halved `cfl` over 500 ticks and passed for the wrong reason (7.6× here;
  0.69× and 1.45× on CI): the Courant step exceeds `dt_ref` until t ≈ 3, so both runs integrated at
  the same dt and the ratio was the chaotic late phase. Now halves the step itself over t ≤ 3 —
  2.00× and 2.01× for two successive halvings, deterministic with threads pinned. Test, gate and
  spec changed together; the threshold (≥ 1.5×) did not.
- The design pass's pre-fix numbers (KE/|U| ≈ 960, "decided in 30 ticks") are lineage, superseded.

POC-07 README carries a forward note; `.spec/challenges.md` C4.1 and C4.4 updated. CLAUDE.md counts.
